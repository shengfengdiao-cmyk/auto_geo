# GEO 自动化定时调度系统 - MVP 原型

## 1. 项目简介
本项目为 `auto_geo` 系统的核心插件原型，旨在实现基于地理位置（GEO）的内容自动化生产与发布闭环。

## 2. 核心功能
- **定时调度**：基于 `APScheduler` 实现，支持按自定义时间点触发任务。
- **任务解耦**：将生成、发布、收录查询逻辑封装为独立函数，方便后期接入正式 API。
- **数据驱动**：支持从 `tasks.json` 动态加载任务列表。

## 3. 快速开始

### 安装依赖
```bash
pip install apscheduler