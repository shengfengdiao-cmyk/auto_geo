# ==================== AutoGeo 环境变量配置模板 ====================
# 小a提醒：复制此文件为 .env 并填入真实值！
# 警告：.env 文件已在 .gitignore 中，不会被提交到 git

# ==================== 加密配置 ====================
# AES-256 加密密钥（32字节）
# 用于加密存储在数据库中的 Cookies 和 localStorage
# 重要：生产环境必须设置自己的密钥！
#
# 生成方法：
#   方法1: python -c "import secrets; print(secrets.token_urlsafe(32))"
#   方法2: 随机输入32个字符
#
AUTO_GEO_ENCRYPTION_KEY=your-32-byte-encryption-key-change-this

# ==================== 服务配置（可选）====================
# 后端服务地址
# HOST=127.0.0.1

# 后端服务端口
# PORT=8001

# 调试模式（生产环境建议设为 false）
# DEBUG=false

# ==================== 数据库配置（可选）====================
# 数据库路径（默认：backend/database/auto_geo_v3.db）
# DATABASE_URL=sqlite:///backend/database/auto_geo_v3.db

# ==================== RAGFlow 配置 ====================
# RAGFlow 服务地址（默认：http://localhost:9380）
RAGFLOW_BASE_URL=http://localhost:9380
# RAGFlow API Key
RAGFLOW_API_KEY=your-ragflow-api-key
# RAGFlow 知识库ID（用于存储采集的文章，若不填会自动创建）
RAGFLOW_DATASET_ID=
# RAGFlow 知识库名称（自动创建时使用）
RAGFLOW_DATASET_NAME=reference_articles_kb
# 去重相似度阈值（默认：0.85）
RAGFLOW_DUPLICATE_THRESHOLD=0.85

# ==================== DeepSeek 配置 ====================
# 用于文章生成和重写
DEEPSEEK_API_KEY=your-deepseek-api-key
DEEPSEEK_API_URL=https://api.deepseek.com/v1
