# ==================== AutoGeo 环境变量配置模板 ====================
# 老王提醒：复制此文件为 .env 并填入真实值！
# 警告：.env 文件已在 .gitignore 中，不会被提交到 git

# ==================== 加密配置 ====================
# AES-256 加密密钥（32字节）
# 用于加密存储在数据库中的 Cookies 和 localStorage
# 重要：生产环境必须设置自己的密钥！
#
# 生成方法：
#   方法1: python -c "import secrets; print(secrets.token_urlsafe(32))"
#   方法2: 随机输入32个字符
#
AUTO_GEO_ENCRYPTION_KEY=your-32-byte-encryption-key-change-this

# ==================== 服务配置（可选）====================
# 后端服务地址
# HOST=127.0.0.1

# 后端服务端口
# PORT=8001

# 调试模式（生产环境建议设为 false）
# DEBUG=false

# ==================== 数据库配置（可选）====================
# 数据库路径（默认：backend/database/auto_geo_v3.db）
# DATABASE_URL=sqlite:///backend/database/auto_geo_v3.db
